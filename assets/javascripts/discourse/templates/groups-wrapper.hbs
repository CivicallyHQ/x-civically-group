<div class='admin-controls'>
  <nav>
    <ul class="nav nav-pills">
      {{#if currentUser.place_category_id}}
        <li>
          {{#link-to "groups" (query-params category_id=currentUser.place_category_id meta=undefined)}}
            {{place-label currentUser.place_category_id}}
          {{/link-to}}
        </li>
      {{/if}}
      <li>
        {{#link-to "groups" (query-params meta=true category_id=undefined)}}
          {{i18n 'groups.civically'}}
        {{/link-to}}
      </li>
    </ul>
  </nav>
</div>
{{partial 'groups/index'}}
